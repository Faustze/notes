// Точка входа для inline-script-loader'а (quartz/cli/handlers.js): он грузит
// как текст только файлы `*.inline.(ts|js)` и бандлит их импорты, поэтому
// файл с этим именем должен существовать. Вся логика — в ../canvas/.
import "../canvas/dappled-scene"
